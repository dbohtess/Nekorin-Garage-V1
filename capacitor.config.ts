import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sultan.nekoringarage',
  appName: 'Nekorin Garage',
  webDir: 'dist'
};

export default config;
